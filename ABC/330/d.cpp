#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,d,p;
    cin >> n >> d >> p;
    int f[n];
    for(int i=0;i<n;i++){
        cin >> f[i];
    }

    // fを大きい順にソート
    sort(f,f+n,greater<int>());
 
    long long ans=0;
    int ave = p / d;

    for(int i=0;i<n;i=i+d){
        if()
    }


    
}
